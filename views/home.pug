extends layout

block content
  if user
    h1 Hi 
      a(href=`/${user.username}`) #{user.name ? user.name : user.username} 
    p Here is a list of all the users in our incredibly huge database
    ul
      each profile in profiles
        li
          a(href=`/${profile.username}`)= profile.name
            
